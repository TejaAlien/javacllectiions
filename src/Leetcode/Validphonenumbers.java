package Leetcode;

public class Validphonenumbers {

    //we are using grep it's a command line utility for searching plain tet data matches with the regular expression
   // ^ ---> means begining
    //$ means ending
    // /(backslash means escape special character)
    //writing an regular expression using grep -P command (-P makes faster)
  //  grep -P '^(\d{3}-|\(\d{3}\)) \d{3}-\d{4}$'
}
